#include "map_utils.h"

using namespace std;

vector<MapBvNode> buildBVH(
	const vector<float>& line_coords,
	vector<int> types,
	BVHType build_type
) {
	vector<MapBvNode> nodes;

	// Do some stuff

	return nodes;
}